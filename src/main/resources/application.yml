management:
  endpoints:
    web:
      exposure:
        include: '*'
  info:
    env:
      enabled: true

info:
  application:
    name: woco-back
    description: REST API
    author: ----------------
    version: '@project.version@'
# change this profile to enable mode development/production
spring:
  profiles:
    active: prod